import 'package:flutter/material.dart';

class Example1RestApi extends StatelessWidget {
  const Example1RestApi({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
